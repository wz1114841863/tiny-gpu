package fetcher

import chisel3._
import chisel3.util._
import statecode.CoreState
import statecode.FetcherState

class Fetcher(ProgramMemAddrBits: Int = 8, ProgramMemAddrBits: Int = 16) extends Module {
    val io = IO(new Bundle {

    })
}
